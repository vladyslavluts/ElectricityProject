@model IEnumerable<ElectricityProject.DataLayer.DbLayer.TypeBuilding>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeBuildingName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeBuildingName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TypeBuildingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TypeBuildingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TypeBuildingId })
        </td>
    </tr>
}

</table>
